Промпт для генерации Docker и README:
Я завершил разработку бэкенд-проекта на FastAPI + SQLAlchemy 2.0 (Async) + PostgreSQL + Alembic + Aiohttp (сервис мониторинга сайтов с фоновым asyncio-воркером и историей проверок).

Помоги мне подготовить файлы для финального деплоя и оформления репозитория:
Dockerfile:
Напиши оптимизированный многослойный Dockerfile для FastAPI-приложения на базе официального образа Python (например, python:3.11-slim), учитывая установку зависимостей через requirements.txt (или poetry/uv, если нужно).
docker-compose.yml:
Создай конфигурацию для поднятия двух сервисов одной командой:
База данных PostgreSQL с persistent volume (чтобы данные не стирались при перезапуске).
Приложение FastAPI, которое автоматически ждет поднятия базы, накатывает миграции через Alembic (alembic upgrade head) и стартует сервер Uvicorn. Пробрось нужные порты и переменные окружения (DATABASE_URL).
README.md:
Напиши крутой, профессиональный файл README для GitHub на русском языке. Он должен содержать:
Краткое описание проекта и его фич (асинхронный движок, JWT-авторизация, фоновый воркер, история проверок).
Стек технологий.
Инструкцию по быстрой установке и запуску через docker compose up --build.
Секцию «Архитектурные решения и Trade-offs», где четко обосновано, почему для фоновых задач был выбран встроенный asyncio-воркер в lifespan вместо тяжелого стека Celery + Redis (отличный аргумент для технических собеседований на стажера/джуна).